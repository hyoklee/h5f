name: mac-14

on:
  push:
    branches:
      - main-
jobs:
  build:
    name: mac-14
    runs-on: macos-14
    permissions:
      contents: read
    steps:
    - name: brew
      run: brew install macfuse

    - name: checkout
      uses: actions/checkout@v6.0.2

    - name: make
      run: |
        make
